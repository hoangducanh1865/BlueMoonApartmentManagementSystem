package com.example.demoapi.model;

public enum ResidentStatus {
    THUONG_TRU,   // Thường trú (Sống lâu dài, có hộ khẩu)
    TAM_TRU,      // Tạm trú (Khách thuê, ở nhờ)
    TAM_VANG,     // Tạm vắng (Đi công tác, đi học xa...)
    DA_CHUYEN_DI  // Đã chuyển đi (Lưu vết lịch sử)
}